---
name: motion-graphics-video
description: 拍に同期した短尺モーショングラフィックス動画を企画・制作・検証する。参考動画や投稿をもとに文字、図形、奥行き、粒子、電子音の演出を再構成してMP4にするときに使う。実写編集や単なる静止画生成には使わない。
---

# Motion Graphics Video

短尺のモーショングラフィックスを、視覚演出と音を共通の拍から組み立てて完成動画まで作る。

## 制作方針

1. 参考URLや添付があれば実物を確認し、画面比率、色、書体、場面転換、動き、音の関係を抽出する。動画を再生できず説明文だけ確認できた場合は、その範囲を明示して進める。参考の固有の文字、ロゴ、音源、画面を無断でそのまま流用せず、演出の仕組みを使って新しい作品にする。
2. 指定された尺、比率、解像度、言語、必須文言、音声の有無を優先する。未指定の要素は用途に合う値を選ぶ。1つの表で「拍／秒／画面／音」を対応させ、主要な変化を同じ時間軸で設計する。
3. 各フレームを `t = frame / fps` から決め、`beat = t * bpm / 60` を映像と音の共通時計にする。場面切替や効果音のタイミングをフレームごとの累積待機や目分量に依存させない。
4. 動きは目的に応じて easing、形状補間、遠近法、粒子の目標位置への補間を選ぶ。質感としてグリッチ、残像、粒子ノイズ、周辺減光などを加える場合も可読性を優先する。
5. レンダリング後に動画全体を確認し、少なくとも開始、中間、場面境界、終盤のフレームを目視する。MP4 の再生時間、解像度、fps、音声トラックを検査し、指定条件との差があれば修正する。

コードで図形・文字を描く場合は、そのままプログラムで制作してよい。AI生成画像やその編集を映像に組み込む場合は、`visual-imagegen-pro` を読んで `imagegen` を使う。

制作手法、拍割りの例、レンダリングと確認の要点は [beat-synced-recipe.md](references/beat-synced-recipe.md) を参照する。

## 納品

完成MP4をユーザーが開ける場所に保存し、リンクを返す。必要なら代表フレームやコンタクトシートも添える。参考資料の確認範囲と、仕様のうち未指定で決めた重要な値を短く説明する。
